; Base environment
[env:base]
platform = espressif32@6.5.0
framework = arduino
monitor_speed = 115200
board_build.partitions = partitions/plane_radar.csv
extra_scripts = post:scripts/merge_firmware.py

; Anti-aliased VLW font (Noto Sans Bold 15, from TFT_eSPI smooth-font examples)
board_build.embed_files = data/ui_font.vlw

build_flags =
  -std=gnu++17
  -DWM_NODEBUG
  -DWM_MDNS

lib_deps =
  lovyan03/LovyanGFX@^1.2.7
  tzapu/WiFiManager@^2.0.17
  bblanchon/ArduinoJson@^7.4.2


; USB serial over USB-C (Super Mini native USB)
[env:base_c3]
extends = env:base

build_flags =
  ${env:base.build_flags}
  -DARDUINO_USB_MODE=1
  -DARDUINO_USB_CDC_ON_BOOT=1


; ============================================================
; ESP32-C3 Super Mini + 1.28" round GC9A01 (240×240)
; Board in Arduino IDE: "ESP32C3 Dev Module" or "MakerGO ESP32 C3 SuperMini"
; ============================================================

[env:supermini]
extends = env:base_c3
board = esp32-c3-devkitm-1

build_flags =
  ${env:base_c3.build_flags}
  -DDISPLAY_TARGET_SUPERMINI


; ============================================================
; Seeed Studio XIAO ESP32-C3
; ============================================================

[env:seeed_xiao]
extends = env:base_c3

board = seeed_xiao_esp32c3

build_flags =
  ${env:base_c3.build_flags}
  -DDISPLAY_TARGET_SEEED_XIAO


; ============================================================
; Sunton ESP32-2432S028R ("Cheap Yellow Display")
; Uses the centered 240×240 portion of the 320×240 ILI9341 panel
; ============================================================

[env:sunton_2432s028r]
extends = env:base

board = esp32dev

build_flags =
  ${env:base.build_flags}
  -DDISPLAY_TARGET_SUNTON_2432S028R